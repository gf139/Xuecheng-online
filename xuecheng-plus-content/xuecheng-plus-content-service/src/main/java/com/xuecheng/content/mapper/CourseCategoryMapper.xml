<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xuecheng.content.mapper.CourseCategoryMapper">

    <select id="selectTreeNodes" resultType="com.xuecheng.model.dto.CourseCategoryTreeDto" parameterType="string">
        WITH RECURSIVE t1 AS (
            SELECT * FROM course_category WHERE id = '1'
            UNION ALL
            SELECT t2.* FROM course_category t2
            INNER JOIN t1 ON t1.id = t2.parentid
        )
        SELECT * FROM t1 ORDER BY t1.id, t1.orderby
    </select>
</mapper>
